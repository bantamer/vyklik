📣 What's new

📊 /stats — the stats are now a clear chart: see at a glance which days and hours the queue is shorter
📌 /dashboard — when a queue is "closed", the called number, your position and the estimate no longer disappear: it's still being served, they just aren't issuing new tickets
❓ /faq — a new section with answers: what "closed" means, how often data refreshes, when alerts are sent
