✨ <b>Vyklik update</b>

📊 <b>/stats</b> — the stats are now a clear chart: see at a glance which days and hours the queue is shorter, and by how much.

📌 <b>/dashboard</b> — when a queue is "closed", the bot no longer hides the data. The called number, your position and the time estimate stay visible — the queue is still being served, they just aren't issuing new tickets.

❓ <b>/faq</b> — a new section with answers: what "closed" means, how often data refreshes, when alerts are sent.

Thanks for using the bot! 💙
